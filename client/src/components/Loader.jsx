import React from "react";
import { Spinner } from "react-bootstrap";

const Loader = () => {
  return <Spinner style={{marginLeft:"47%",marginTop:"50px"}} animation="border" variant="info" />;
};

export default Loader;
